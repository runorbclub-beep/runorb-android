<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <!--<item android:id="@+id/action_notify"
        android:title="@string/lbl_notify"
        android:icon="@mipmap/menu_icon_notify"
        app:actionProviderClass="com.cloud.runball.widget.BadgeActionProvider"
        android:orderInCategory="99"
        app:showAsAction="ifRoom"/>-->

    <item android:id="@+id/action_setting"
        android:title="@string/lbl_setting"
        android:icon="@mipmap/menu_icon_setting"
        android:orderInCategory="100"
        app:showAsAction="ifRoom"/>
</menu>